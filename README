# Server for dropsy client

   USAGE

   Create TLS certificate and keys with "keys_config.sh"

   To add users to the system to each line of config/passwd
   add:

   "username:password\n"

   run "make"

